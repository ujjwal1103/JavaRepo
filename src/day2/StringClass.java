package day2;

public class StringClass {
    public StringClass() {

    }

    public static void main(String[] args) {
        String str1 = "ujjwal";
        String str2 = "lade";
        System.out.println(str1.compareTo(str2));
        System.out.println(str1.compareTo(str2));
        System.out.println(str1.compareTo(str2));
        System.out.println(str1.compareTo(str2));
        System.out.println(str1.compareTo(str2));

        System.out.println(str1.compareTo(str2));
        System.out.println(str1.compareTo(str2));
        System.out.println(str1.compareTo(str2));




        System.out.println(str1.compareTo(str2));
        System.out.println(str1.compareTo(str2));
        String str = "";

        System.out.println();
//        or loop  --> Two numbers are entered through the keyboard. Write a program
//        to find the value of one number raised to the power of another.
//        (Do not use Java built-in method)
//        do..while().. --> Write a program that generates
//        a random number and asks the user to guess what the
//        number is. If the user's guess is higher than the random
//        number, the program should display "Too high, try again."
//        If the user's guess is lower than the random number,
//        the program should display "Too low, try again." The
//        program should use a loop that repeats until the user correctly guesses the random number.

//        while --> Write a program that prompts the user to input an integer and
//        then outputs the number with the digits reversed. For example, if the
//        input is 12345, the output should be 54321.


    }
}
